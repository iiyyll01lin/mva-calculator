server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index l6-ddm-demo.html;

  # Serve the demo at root
  location / {
    try_files $uri $uri/ /l6-ddm-demo.html;
  }

  # Keep dev-friendly caching
  location ~* \.(js|css|html)$ {
    add_header Cache-Control "no-store";
  }
}
